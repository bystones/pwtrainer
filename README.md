# pwtrainer

Small utility to help learn a new password. Upon running the utility you will
be asked to enter the "correct" password. Afterwards you have to re-enter the
password from memory in an infinite loop. 

On each attempt a small hint is shown between the password ind the current
input.
